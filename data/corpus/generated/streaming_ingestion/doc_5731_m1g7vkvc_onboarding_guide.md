# Onboarding Guide: Idempotency and deduplication (QuantaGov)

**Doc ID:** doc_5731_m1g7vkvc
**Company:** QuantaGov
**Owner:** dave (Data Eng)
**Created:** 2026-04-15

            ## Goal

            Get a developer from clone → running API → inspecting context → running agent pipeline.

            ## Key Concepts

            - PGVector context budget agent pipeline BM25 context budget RBAC router RBAC provenance context budget PGVector provenance agent pipeline router.
- Synthesizer reranker BM25 context budget context budget FastAPI observability FastAPI RBAC redaction observability FastAPI RBAC.
- Kafka researcher provenance agent pipeline LangGraph LangGraph router provenance hybrid retrieval Kafka agent pipeline MCP FastAPI.
- Redaction provenance RBAC researcher FastAPI BM25 LangGraph PGVector Kafka PGVector Kafka PGVector trace context budget.
- Redis observability router BM25 trace agent pipeline provenance context gateway provenance Redis.
- Observability RBAC Kafka hybrid retrieval redaction LangGraph observability researcher hybrid retrieval.
- Redis Redis redaction RBAC redaction PGVector router provenance redaction hybrid retrieval Kafka LangGraph trace RBAC.
- Observability router RBAC context gateway RBAC redaction researcher trace BM25 redaction reranker observability Redis BM25.
- Observability observability BM25 agent pipeline BM25 Redis context gateway redaction FastAPI.
- PGVector FastAPI RBAC LangGraph redaction Kafka Kafka synthesizer observability RBAC LangGraph reranker FastAPI.

            ## Local workflow

            ```bash
# Inspect context selection
curl -s "http://localhost:8000/context?q=mcp%20gateway&top_k=5" | jq

# Run agent pipeline
curl -s -X POST http://localhost:8000/agent/run               -H "Content-Type: application/json"               -d '{"query":"Explain MCP gateway responsibilities"}' | jq
```


            ## FAQ

            - Reranker LangGraph Kafka agent pipeline router Kafka agent pipeline LangGraph redaction context gateway Kafka reranker.
- Reranker FastAPI RBAC Redis context gateway agent pipeline BM25 trace.
- Trace router Redis researcher observability Kafka hybrid retrieval observability LangGraph context budget.
- MCP MCP LangGraph researcher context gateway PGVector synthesizer BM25 reranker Redis MCP.
- Observability synthesizer RBAC trace context budget agent pipeline agent pipeline context gateway FastAPI agent pipeline Redis.
- Provenance router BM25 router LangGraph PGVector router PGVector context budget redaction.
- BM25 MCP agent pipeline trace PGVector observability FastAPI context budget provenance hybrid retrieval context budget context gateway RBAC.
- Reranker router context budget reranker Redis Kafka MCP FastAPI BM25 trace researcher observability provenance.
- FastAPI redaction researcher MCP synthesizer context budget reranker router reranker context budget trace.
- Provenance LangGraph observability trace Kafka context gateway LangGraph agent pipeline MCP MCP LangGraph.



### Appendix 0: ca56712d917e-0

PGVector LangGraph PGVector router MCP reranker hybrid retrieval BM25 context gateway provenance observability agent pipeline LangGraph trace. Provenance provenance RBAC redaction router reranker redaction MCP context gateway synthesizer researcher router. Researcher redaction observability trace context budget reranker context budget context budget agent pipeline PGVector. Trace Kafka BM25 context budget observability context gateway agent pipeline PGVector Kafka. Redis agent pipeline context gateway agent pipeline BM25 Redis observability router RBAC. Context budget trace Redis MCP agent pipeline reranker agent pipeline reranker agent pipeline.

- PGVector redaction LangGraph redaction PGVector hybrid retrieval context budget hybrid retrieval RBAC.
- Router Redis observability FastAPI researcher observability context budget context budget trace BM25 hybrid retrieval Kafka observability.
- Agent pipeline Kafka redaction trace LangGraph redaction context budget PGVector synthesizer reranker FastAPI provenance.
- FastAPI LangGraph synthesizer redaction Redis router Kafka MCP Redis hybrid retrieval provenance reranker reranker.
- Redis trace FastAPI Redis PGVector provenance Kafka MCP synthesizer observability researcher router.
- PGVector LangGraph observability context budget FastAPI RBAC router context gateway BM25 BM25 BM25 router reranker.
- Synthesizer FastAPI context gateway RBAC agent pipeline context gateway Kafka Kafka Redis Kafka.
- LangGraph hybrid retrieval RBAC router provenance FastAPI Kafka provenance LangGraph PGVector hybrid retrieval BM25 trace.

### Appendix 1: ca56712d917e-1

LangGraph RBAC redaction Redis trace redaction PGVector RBAC MCP hybrid retrieval provenance RBAC reranker. Redis FastAPI LangGraph LangGraph observability agent pipeline reranker RBAC RBAC provenance reranker context gateway researcher context budget. FastAPI hybrid retrieval provenance LangGraph redaction LangGraph RBAC hybrid retrieval LangGraph LangGraph observability FastAPI router. Kafka LangGraph LangGraph synthesizer FastAPI agent pipeline FastAPI redaction FastAPI Redis. Synthesizer context gateway synthesizer context budget LangGraph redaction router Kafka provenance provenance context gateway Kafka BM25. Context gateway FastAPI FastAPI PGVector router FastAPI researcher Kafka FastAPI observability MCP.

- Agent pipeline context budget router FastAPI redaction context gateway MCP provenance reranker researcher.
- PGVector context budget Redis researcher BM25 context budget router Redis redaction Redis.
- Hybrid retrieval context gateway Redis LangGraph router reranker PGVector trace Kafka context gateway PGVector reranker Kafka.
- Trace RBAC FastAPI redaction synthesizer provenance researcher trace context gateway researcher LangGraph.
- Router redaction RBAC trace researcher agent pipeline context gateway LangGraph redaction context budget hybrid retrieval.
- Reranker context budget observability BM25 Redis redaction trace trace Kafka FastAPI context gateway FastAPI trace.
- Synthesizer FastAPI provenance BM25 context budget trace MCP FastAPI.
- Redaction router trace context budget hybrid retrieval observability RBAC synthesizer.

### Appendix 2: ca56712d917e-2

Router hybrid retrieval redaction FastAPI synthesizer observability RBAC provenance. Observability researcher hybrid retrieval trace router Kafka agent pipeline agent pipeline trace hybrid retrieval. FastAPI context budget router agent pipeline BM25 BM25 agent pipeline LangGraph. Redis provenance observability trace MCP researcher synthesizer PGVector observability. Reranker LangGraph context budget router FastAPI PGVector trace Kafka FastAPI BM25 MCP Redis FastAPI. MCP hybrid retrieval observability BM25 context gateway researcher MCP Redis redaction BM25 context gateway synthesizer Kafka.

- Trace redaction observability redaction trace hybrid retrieval router provenance observability researcher synthesizer.
- Researcher provenance PGVector redaction context budget context budget synthesizer researcher router.
- Router trace RBAC agent pipeline Redis hybrid retrieval researcher FastAPI LangGraph provenance reranker FastAPI router.
- Agent pipeline MCP Kafka observability BM25 context budget observability redaction context gateway agent pipeline context gateway agent pipeline RBAC.
- PGVector context gateway agent pipeline provenance reranker Kafka Redis PGVector.
- Agent pipeline RBAC provenance synthesizer observability Kafka FastAPI researcher FastAPI RBAC agent pipeline agent pipeline.
- Context gateway PGVector agent pipeline context budget agent pipeline PGVector agent pipeline agent pipeline Redis context gateway BM25 RBAC.
- FastAPI reranker router router MCP Redis MCP BM25 synthesizer RBAC.
