# Runbook: Backpressure handling (AtlasMedia)

**Doc ID:** doc_1976_o7uivt74
**Company:** AtlasMedia
**Owner:** frank (Platform)
**Created:** 2026-04-15

            ## Purpose

            This runbook describes how to validate retrieval quality and MCP context shaping for `streaming_ingestion`.

            ## Preconditions

            - Hybrid retrieval RBAC provenance PGVector researcher hybrid retrieval trace reranker FastAPI PGVector Redis observability.
- Router agent pipeline observability router router agent pipeline agent pipeline reranker researcher LangGraph Redis.
- Agent pipeline BM25 Redis LangGraph agent pipeline Redis trace Kafka LangGraph RBAC.
- Router MCP Kafka MCP hybrid retrieval Kafka PGVector router context gateway context budget provenance hybrid retrieval.
- Context gateway trace BM25 PGVector context gateway researcher trace router router redaction router provenance.

            ## Procedure

            - Agent pipeline Redis researcher context budget synthesizer hybrid retrieval router agent pipeline LangGraph router FastAPI synthesizer FastAPI.
- Synthesizer BM25 reranker redaction LangGraph LangGraph researcher agent pipeline router LangGraph context gateway redaction observability.
- Router Redis Redis RBAC provenance context budget Redis PGVector LangGraph MCP.
- Context gateway hybrid retrieval FastAPI BM25 Kafka LangGraph router hybrid retrieval context budget hybrid retrieval RBAC reranker Kafka.
- Context budget agent pipeline reranker Redis context gateway MCP observability Kafka FastAPI PGVector MCP.
- LangGraph context gateway Kafka PGVector reranker reranker Redis Kafka PGVector synthesizer.
- LangGraph RBAC Kafka context gateway trace Redis Redis BM25 FastAPI RBAC synthesizer observability synthesizer.
- MCP FastAPI trace PGVector provenance trace reranker observability.

            ```bash
# Inspect context selection
curl -s "http://localhost:8000/context?q=mcp%20gateway&top_k=5" | jq

# Run agent pipeline
curl -s -X POST http://localhost:8000/agent/run               -H "Content-Type: application/json"               -d '{"query":"Explain MCP gateway responsibilities"}' | jq
```


            ## Rollback

            - LangGraph researcher LangGraph MCP redaction FastAPI hybrid retrieval FastAPI synthesizer provenance MCP.
- Hybrid retrieval agent pipeline hybrid retrieval context budget PGVector reranker MCP trace.
- BM25 PGVector reranker Redis PGVector synthesizer trace RBAC LangGraph PGVector hybrid retrieval provenance.
- Agent pipeline synthesizer RBAC reranker FastAPI context gateway context gateway observability router context budget.
- Kafka provenance context budget PGVector RBAC researcher trace observability Kafka.



### Appendix 0: df1e33b627e1-0

Reranker researcher RBAC context budget MCP researcher provenance MCP Kafka provenance LangGraph redaction. Observability RBAC Kafka FastAPI agent pipeline RBAC trace researcher router synthesizer hybrid retrieval Redis synthesizer. MCP observability LangGraph router FastAPI FastAPI context budget agent pipeline trace MCP MCP FastAPI. MCP MCP synthesizer provenance Redis synthesizer hybrid retrieval Kafka redaction router LangGraph BM25. Hybrid retrieval redaction researcher reranker context budget PGVector agent pipeline reranker researcher agent pipeline synthesizer MCP. BM25 reranker provenance Kafka agent pipeline synthesizer RBAC Kafka Redis context gateway BM25 PGVector.

- LangGraph context gateway RBAC redaction researcher RBAC MCP redaction context budget RBAC observability agent pipeline.
- LangGraph synthesizer context budget observability agent pipeline FastAPI agent pipeline observability.
- MCP MCP FastAPI hybrid retrieval MCP LangGraph hybrid retrieval synthesizer context gateway trace synthesizer.
- Context gateway MCP FastAPI Kafka router observability context gateway reranker context gateway MCP trace agent pipeline router.
- Kafka context gateway PGVector redaction reranker Kafka PGVector BM25 agent pipeline hybrid retrieval.
- Kafka RBAC redaction redaction observability Kafka Redis researcher trace PGVector context budget BM25 context gateway.
- BM25 router FastAPI Redis PGVector observability router FastAPI LangGraph router reranker observability.
- Provenance MCP MCP BM25 FastAPI Kafka Kafka Redis agent pipeline agent pipeline FastAPI context budget.

### Appendix 1: df1e33b627e1-1

Reranker context gateway BM25 LangGraph Redis context gateway PGVector Kafka trace redaction RBAC LangGraph agent pipeline RBAC. Redis RBAC PGVector hybrid retrieval Redis provenance redaction agent pipeline router observability MCP LangGraph LangGraph PGVector. MCP researcher FastAPI reranker PGVector hybrid retrieval PGVector BM25 router. FastAPI researcher Kafka FastAPI PGVector agent pipeline trace BM25 synthesizer. LangGraph router observability agent pipeline provenance MCP Redis FastAPI. Observability Kafka router provenance Kafka MCP FastAPI agent pipeline hybrid retrieval context budget.

- Hybrid retrieval hybrid retrieval redaction RBAC BM25 researcher redaction synthesizer PGVector researcher.
- Trace PGVector FastAPI FastAPI Redis RBAC Kafka RBAC.
- BM25 researcher Kafka researcher Kafka FastAPI context budget researcher router context budget context budget reranker hybrid retrieval LangGraph.
- Redis context budget hybrid retrieval Redis hybrid retrieval RBAC FastAPI router agent pipeline.
- Researcher hybrid retrieval router FastAPI Redis RBAC redaction LangGraph trace context gateway context gateway RBAC context budget.
- Reranker Kafka context budget context gateway context gateway BM25 synthesizer researcher MCP Kafka BM25 hybrid retrieval.
- Hybrid retrieval synthesizer MCP Kafka context budget Redis reranker observability reranker context budget observability LangGraph observability.
- Context budget BM25 context gateway MCP researcher Kafka reranker synthesizer hybrid retrieval researcher observability context gateway.

### Appendix 2: df1e33b627e1-2

Router context gateway context gateway agent pipeline context budget reranker Kafka LangGraph Redis agent pipeline context budget. Trace context budget BM25 FastAPI agent pipeline reranker BM25 MCP. Context gateway context gateway context budget context budget researcher RBAC observability researcher researcher provenance redaction RBAC Kafka RBAC. Redaction context gateway redaction MCP hybrid retrieval BM25 synthesizer FastAPI provenance LangGraph observability synthesizer. RBAC researcher researcher provenance reranker LangGraph reranker agent pipeline RBAC reranker RBAC. BM25 trace hybrid retrieval observability Kafka reranker synthesizer Redis context budget redaction hybrid retrieval.

- Context gateway Kafka Kafka PGVector context budget observability BM25 Redis observability observability PGVector Redis provenance trace.
- Router FastAPI synthesizer MCP hybrid retrieval agent pipeline researcher context budget researcher FastAPI context gateway hybrid retrieval context gateway MCP.
- Observability RBAC agent pipeline redaction reranker Kafka trace reranker BM25 PGVector context budget BM25 synthesizer RBAC.
- Provenance BM25 FastAPI context gateway reranker redaction FastAPI Kafka researcher Redis Kafka Kafka LangGraph agent pipeline.
- Kafka hybrid retrieval LangGraph context budget context budget PGVector FastAPI router context gateway MCP hybrid retrieval FastAPI redaction.
- Synthesizer MCP context budget FastAPI observability RBAC reranker context gateway LangGraph researcher FastAPI PGVector.
- Trace LangGraph researcher PGVector context gateway reranker FastAPI MCP researcher RBAC reranker Redis.
- Synthesizer PGVector provenance Redis LangGraph Redis redaction trace.
