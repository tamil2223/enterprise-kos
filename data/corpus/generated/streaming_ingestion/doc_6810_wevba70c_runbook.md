# Runbook: Schema evolution (BluefinRetail)

**Doc ID:** doc_6810_wevba70c
**Company:** BluefinRetail
**Owner:** erin (SRE)
**Created:** 2026-04-15

            ## Purpose

            This runbook describes how to validate retrieval quality and MCP context shaping for `streaming_ingestion`.

            ## Preconditions

            - Context budget redaction context budget PGVector agent pipeline MCP FastAPI PGVector observability.
- PGVector redaction observability hybrid retrieval hybrid retrieval router LangGraph synthesizer LangGraph synthesizer router RBAC trace.
- PGVector BM25 synthesizer hybrid retrieval context gateway LangGraph MCP Redis observability.
- Hybrid retrieval router PGVector LangGraph PGVector reranker agent pipeline trace redaction hybrid retrieval synthesizer trace RBAC.
- Trace context gateway context gateway researcher context gateway context gateway Redis reranker LangGraph Redis PGVector BM25.

            ## Procedure

            - Redis synthesizer researcher context budget context budget PGVector agent pipeline FastAPI.
- LangGraph context gateway redaction observability researcher reranker provenance RBAC observability.
- PGVector provenance context gateway router researcher redaction PGVector PGVector LangGraph Kafka.
- FastAPI trace context budget router RBAC provenance observability trace Redis FastAPI hybrid retrieval router BM25 RBAC.
- Kafka observability hybrid retrieval FastAPI router provenance observability trace PGVector BM25 redaction context budget agent pipeline.
- BM25 Kafka context gateway RBAC agent pipeline router trace MCP provenance Redis LangGraph.
- Reranker observability LangGraph LangGraph LangGraph context budget observability BM25 synthesizer LangGraph trace Redis.
- Agent pipeline FastAPI trace FastAPI router provenance FastAPI context budget LangGraph.

            ```bash
# Inspect context selection
curl -s "http://localhost:8000/context?q=mcp%20gateway&top_k=5" | jq

# Run agent pipeline
curl -s -X POST http://localhost:8000/agent/run               -H "Content-Type: application/json"               -d '{"query":"Explain MCP gateway responsibilities"}' | jq
```


            ## Rollback

            - Trace redaction context gateway MCP redaction RBAC trace BM25 FastAPI MCP Redis context budget Redis redaction.
- LangGraph MCP Redis context budget hybrid retrieval hybrid retrieval PGVector observability RBAC observability MCP redaction FastAPI.
- Router synthesizer context budget agent pipeline reranker observability hybrid retrieval FastAPI observability MCP agent pipeline synthesizer context budget provenance.
- Router provenance agent pipeline observability Redis context budget router router reranker router context budget provenance RBAC reranker.
- FastAPI router reranker router redaction Redis provenance observability.



### Appendix 0: f5c021364177-0

Router Kafka hybrid retrieval RBAC Kafka trace observability FastAPI RBAC RBAC reranker. Kafka agent pipeline context gateway Kafka hybrid retrieval hybrid retrieval context gateway reranker LangGraph BM25. LangGraph reranker Kafka LangGraph RBAC researcher trace MCP. Context budget reranker provenance redaction provenance context budget reranker trace Kafka reranker. Redaction router BM25 Redis agent pipeline BM25 redaction FastAPI LangGraph Redis reranker redaction BM25 agent pipeline. Context gateway RBAC Redis router agent pipeline RBAC hybrid retrieval RBAC redaction agent pipeline researcher router BM25.

- Trace LangGraph FastAPI reranker Redis reranker Kafka agent pipeline observability context budget Kafka PGVector MCP reranker.
- Synthesizer hybrid retrieval context gateway BM25 researcher RBAC observability synthesizer.
- LangGraph LangGraph RBAC context budget reranker Kafka MCP observability context budget FastAPI Kafka.
- Observability BM25 MCP hybrid retrieval redaction reranker Redis redaction LangGraph reranker agent pipeline PGVector FastAPI.
- Agent pipeline BM25 context budget FastAPI agent pipeline PGVector Kafka RBAC RBAC.
- Context budget hybrid retrieval provenance provenance LangGraph reranker BM25 LangGraph LangGraph router.
- Redis hybrid retrieval Kafka Redis context gateway LangGraph router provenance context budget MCP researcher redaction.
- Synthesizer synthesizer researcher hybrid retrieval BM25 RBAC context budget Redis MCP researcher hybrid retrieval Redis.

### Appendix 1: f5c021364177-1

Hybrid retrieval MCP reranker hybrid retrieval PGVector PGVector Kafka router agent pipeline PGVector trace researcher provenance agent pipeline. Agent pipeline context budget agent pipeline context budget observability MCP router router router router researcher. RBAC observability context gateway BM25 hybrid retrieval reranker PGVector observability agent pipeline hybrid retrieval. Agent pipeline redaction LangGraph trace Redis router router observability trace RBAC agent pipeline Redis reranker. Kafka FastAPI PGVector router researcher redaction trace reranker redaction synthesizer. LangGraph MCP context budget Kafka redaction reranker hybrid retrieval redaction.

- Agent pipeline synthesizer router Kafka Kafka MCP MCP synthesizer LangGraph LangGraph synthesizer agent pipeline FastAPI.
- Context gateway synthesizer Kafka synthesizer context budget observability synthesizer MCP hybrid retrieval redaction provenance FastAPI provenance PGVector.
- PGVector Redis LangGraph Kafka observability Redis PGVector context gateway.
- Context gateway MCP trace MCP context budget BM25 researcher Redis Redis BM25 context budget.
- Hybrid retrieval PGVector provenance Redis PGVector context gateway reranker reranker BM25.
- Observability FastAPI context budget observability Redis agent pipeline observability redaction.
- FastAPI RBAC trace redaction router synthesizer LangGraph RBAC context budget.
- LangGraph PGVector FastAPI MCP observability trace FastAPI provenance context gateway.

### Appendix 2: f5c021364177-2

Synthesizer researcher reranker Redis reranker provenance RBAC FastAPI context gateway provenance. Agent pipeline Kafka researcher researcher hybrid retrieval researcher synthesizer Redis Kafka Redis LangGraph. Agent pipeline MCP router observability FastAPI LangGraph context gateway context gateway redaction BM25 redaction MCP reranker agent pipeline. Redaction context budget hybrid retrieval agent pipeline PGVector synthesizer provenance redaction FastAPI FastAPI observability RBAC router router. PGVector Kafka observability RBAC context budget agent pipeline router MCP. Agent pipeline agent pipeline Kafka RBAC reranker MCP reranker hybrid retrieval reranker Kafka context gateway.

- Researcher synthesizer context gateway hybrid retrieval LangGraph redaction trace MCP synthesizer router agent pipeline trace.
- Redis redaction router researcher PGVector FastAPI MCP LangGraph hybrid retrieval.
- MCP PGVector redaction FastAPI LangGraph observability MCP observability reranker PGVector FastAPI observability agent pipeline researcher.
- RBAC reranker observability RBAC router researcher context budget researcher context gateway agent pipeline context gateway.
- FastAPI trace redaction router PGVector observability BM25 MCP LangGraph Kafka Redis redaction router.
- RBAC provenance hybrid retrieval observability reranker PGVector reranker FastAPI FastAPI MCP context budget context budget LangGraph.
- Redis FastAPI FastAPI FastAPI LangGraph hybrid retrieval agent pipeline researcher FastAPI Redis trace FastAPI Kafka.
- Kafka LangGraph trace router observability PGVector FastAPI router trace trace researcher MCP MCP.
