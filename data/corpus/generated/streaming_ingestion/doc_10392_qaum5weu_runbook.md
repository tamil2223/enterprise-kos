# Runbook: Backpressure handling (BluefinRetail)

**Doc ID:** doc_10392_qaum5weu
**Company:** BluefinRetail
**Owner:** erin (SRE)
**Created:** 2026-04-15

            ## Purpose

            This runbook describes how to validate retrieval quality and MCP context shaping for `streaming_ingestion`.

            ## Preconditions

            - MCP LangGraph synthesizer RBAC PGVector hybrid retrieval router router hybrid retrieval.
- FastAPI MCP researcher reranker LangGraph context gateway agent pipeline Kafka router redaction context budget RBAC MCP context gateway.
- Context budget synthesizer RBAC reranker RBAC MCP synthesizer LangGraph router LangGraph router provenance.
- Redis synthesizer hybrid retrieval router MCP context gateway hybrid retrieval context budget LangGraph trace PGVector.
- Context gateway redaction agent pipeline Redis observability redaction Redis observability.

            ## Procedure

            - PGVector researcher observability context gateway trace hybrid retrieval redaction RBAC PGVector LangGraph context gateway reranker PGVector Redis.
- Router router context budget context budget Redis Kafka Kafka redaction observability researcher.
- LangGraph PGVector hybrid retrieval agent pipeline trace LangGraph provenance MCP router Kafka Kafka.
- Kafka LangGraph context budget trace PGVector context budget observability Redis context budget router.
- LangGraph RBAC trace trace redaction observability Kafka reranker redaction.
- BM25 BM25 Redis observability PGVector redaction trace reranker synthesizer.
- Hybrid retrieval BM25 observability Kafka trace context budget FastAPI context budget observability Redis.
- LangGraph redaction Redis LangGraph Kafka context gateway agent pipeline LangGraph.

            ```bash
# Inspect context selection
curl -s "http://localhost:8000/context?q=mcp%20gateway&top_k=5" | jq

# Run agent pipeline
curl -s -X POST http://localhost:8000/agent/run               -H "Content-Type: application/json"               -d '{"query":"Explain MCP gateway responsibilities"}' | jq
```


            ## Rollback

            - Hybrid retrieval LangGraph LangGraph context gateway Redis LangGraph redaction trace context budget Kafka trace Redis context gateway synthesizer.
- MCP provenance context gateway reranker PGVector Kafka FastAPI hybrid retrieval LangGraph reranker router trace provenance provenance.
- BM25 Kafka agent pipeline PGVector agent pipeline PGVector MCP redaction.
- Kafka hybrid retrieval hybrid retrieval MCP LangGraph redaction redaction redaction LangGraph.
- Provenance synthesizer context budget trace FastAPI reranker Kafka agent pipeline.



### Appendix 0: a626aaa3630f-0

RBAC agent pipeline hybrid retrieval synthesizer RBAC agent pipeline router context gateway MCP observability provenance PGVector. PGVector RBAC Kafka redaction PGVector Redis reranker hybrid retrieval context gateway trace RBAC router FastAPI. Hybrid retrieval FastAPI reranker MCP trace router RBAC RBAC FastAPI PGVector FastAPI context gateway. Agent pipeline agent pipeline context budget reranker provenance trace context gateway router Redis. Provenance hybrid retrieval MCP reranker redaction MCP Kafka provenance context gateway. Synthesizer reranker synthesizer BM25 LangGraph MCP context budget context budget PGVector RBAC redaction provenance trace Redis.

- FastAPI MCP router researcher hybrid retrieval agent pipeline researcher context budget observability router.
- Agent pipeline reranker redaction hybrid retrieval context gateway researcher agent pipeline MCP hybrid retrieval LangGraph context budget FastAPI context budget.
- LangGraph RBAC researcher provenance researcher BM25 observability BM25.
- Researcher observability Kafka provenance reranker MCP RBAC synthesizer researcher trace trace Redis router provenance.
- Context gateway provenance router BM25 redaction MCP RBAC agent pipeline RBAC.
- Kafka Kafka context budget context gateway BM25 FastAPI researcher BM25 Redis researcher FastAPI FastAPI synthesizer researcher.
- BM25 provenance context budget router hybrid retrieval observability researcher FastAPI redaction.
- Redaction reranker trace context gateway LangGraph synthesizer provenance hybrid retrieval MCP hybrid retrieval PGVector RBAC BM25.

### Appendix 1: a626aaa3630f-1

Researcher MCP provenance Redis redaction synthesizer trace synthesizer LangGraph router Kafka RBAC RBAC context gateway. Router PGVector router context gateway Redis RBAC LangGraph Kafka reranker PGVector. RBAC Kafka BM25 hybrid retrieval hybrid retrieval reranker PGVector LangGraph. PGVector router reranker agent pipeline trace agent pipeline reranker LangGraph context gateway MCP. RBAC RBAC redaction Kafka Kafka agent pipeline router RBAC. Provenance Kafka observability observability LangGraph LangGraph redaction researcher context budget.

- Agent pipeline researcher context gateway context budget context gateway observability Kafka RBAC agent pipeline researcher context gateway LangGraph.
- FastAPI reranker provenance hybrid retrieval Kafka synthesizer Redis hybrid retrieval Kafka observability Redis trace agent pipeline.
- LangGraph context budget PGVector Redis BM25 MCP reranker trace reranker.
- Kafka PGVector MCP context gateway provenance redaction context budget RBAC PGVector FastAPI context gateway Kafka Kafka provenance.
- RBAC redaction BM25 context gateway agent pipeline researcher RBAC context budget.
- FastAPI synthesizer reranker BM25 RBAC context gateway Redis PGVector context gateway hybrid retrieval.
- Hybrid retrieval trace router synthesizer PGVector FastAPI redaction LangGraph hybrid retrieval MCP LangGraph provenance LangGraph.
- FastAPI Redis redaction hybrid retrieval observability Redis context budget observability MCP provenance.
